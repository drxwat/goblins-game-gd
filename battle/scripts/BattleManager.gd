extends Spatial

var units: Array = []

func _ready():
	var units = get_children()

func initialize_battle(units: Array, spawn_points: Array):
	pass

func spawn_units(units: Array, spawn_start: Vector3, spawn_step: float):
	pass
